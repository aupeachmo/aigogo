# Rendering the slides

```bash
npm install
npm run dev
```

### Without Node installed (Docker)

```bash
bash build.sh
bash serve.sh
```

This builds the slides in a container, copies the static output to `slides/`, and serves it locally.
